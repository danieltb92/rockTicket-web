---
export interface CardProp {
  title: string;
}
// Proporcionar valores predeterminados si no se pasan props
  const title = Astro.props.title || 'Descarga al App';
---                   


<a
  class=" font-geist font-normal text-base leading-[120%]flex flex-wrap w-fit justify-between px-5 py-3 bg-cyan-950 text-white rounded-md"
  href="">{title}</a
>
